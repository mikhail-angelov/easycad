# SPEC11 CadQuery Chat backend.
# Runtime venv (.venv-poc) also needs cadquery, which is heavy (OCP) and is
# typically installed separately: `uv pip install cadquery`.
fastapi
uvicorn[standard]
pydantic
openai
python-dotenv
cadquery
# easycad_geom + bench harness (shared measurement package, bench-SPEC §2.7, §6).
# trimesh: mesh normalization, watertight, surface-to-surface distance.
# pyyaml:  scenario.yaml parsing. matplotlib (renders) is optional — render.py
# degrades gracefully when it is absent.
trimesh
pyyaml
# dev
pytest
